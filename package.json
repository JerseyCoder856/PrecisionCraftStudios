{
  "name": "precision-craft-studios",
  "version": "1.0.0",
  "private": true,
  "description": "Precision Craft Studios storefront with server-side PayPal Checkout integration.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "migrate": "node scripts/migrate.js",
    "test": "node --test"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=24.0.0"
  }
}
